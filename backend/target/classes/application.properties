server.port=8080
spring.application.name=weather-app

# Allow CORS from anywhere (tighten in production)
spring.web.cors.allowed-origins=*

# Increase WebClient timeout
spring.webflux.client.connect-timeout=10000
spring.webflux.client.read-timeout=10000

logging.level.com.weather=DEBUG
